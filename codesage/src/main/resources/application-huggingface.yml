# Hugging Face Inference API — get token at https://huggingface.co/settings/tokens
codesage:
  embedding:
    provider: HUGGINGFACE
    api-key: ${HUGGINGFACE_API_KEY:}
    model: BAAI/bge-small-en-v1.5
    dimensions: 384

spring:
  flyway:
    placeholders:
      embedding_dimensions: 384
